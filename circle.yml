machine:
  services:
    - redis
  node:
    version: 7.6.0
deployment:
  release:
    tag: /release-.*/
    owner: redirectpro
    commands:
      - echo "release"
  development:
    branch: master
    commands:
      # - aws configure set region eu-central-1
      - ./deploy.sh
